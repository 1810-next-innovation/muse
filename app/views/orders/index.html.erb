<h1>購入履歴</h1>

<%= @orders.each do |order| %>
	<%= order.created_at %>
	<%= order.cart.cart_items.each do |cart_item| %>
		<%= cart_item.item.item_name %>
		<%= cart_item.quantity %>
	<% end %>
<% end %>
