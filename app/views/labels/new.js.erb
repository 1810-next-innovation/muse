// モーダル機能実装のための候補コードだったが、
// 他の機能が干渉してしまうため実装できず廃棄。
// 再度利用の可能性は残っているため保存中

$("#label-modal").html("<%= escape_javascript(render 'new') %>");　//labels内の_newを表示する
$("#label-modal").modal("show");
// $("#label-modal").show();
